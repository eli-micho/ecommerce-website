import React from 'react';
import Checkout from '../../components/Checkout';

const Cart = () => {
    return (
        <section>
            <Checkout/>
        </section>
    );
};

export default Cart;
